version: '3.8'

services:
  api:
    build: .
    ports:
      - "${PORT:-8001}:${PORT:-8001}"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${PORT:-8001}/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
